# Copy this file to .env for local development. Never commit .env.
GODADDY_SMTP_PASSWORD=replace_with_your_password

# Optional overrides (the defaults are appropriate for reachbeyondusa.org).
# APP_HOST=reachbeyondusa.org
# GODADDY_SMTP_ADDRESS=smtpout.secureserver.net
# GODADDY_SMTP_PORT=587
